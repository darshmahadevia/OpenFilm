# Use a focused Look model

A Look contains exposure, contrast, temperature, tint, saturation, fade, one RGB tone curve, vignette, and grain. Geometry stays in the Edit. This set is large enough to demonstrate shader work and custom controls while remaining feasible for one student to implement and test over a semester.
