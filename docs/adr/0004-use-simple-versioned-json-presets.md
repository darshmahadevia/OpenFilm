# Use simple versioned JSON presets

Preset files use a small versioned JSON object with a title, optional description, and supported Look values. Runtime validation rejects malformed or unsupported files. OpenFilm does not promise canonical serialization, extension preservation, content hashes, or compatibility with independent implementations.
