# Use static Vite and GitHub Pages

OpenFilm is a Vite, React, and TypeScript application deployed from its public repository to GitHub Pages. It has no server runtime, database, service worker, or required external API. This keeps hosting free and leaves deployment easy for a student to explain and maintain.
