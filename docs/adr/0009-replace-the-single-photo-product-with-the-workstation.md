---
status: accepted
---

# Replace the single-photo product with the workstation

OpenFilm v2 replaces the current single-photo editor in this repository with a Library-based workstation. It reuses suitable rendering and Edit code instead of maintaining the old editor as a separate mode, because two products would require competing navigation, state models, and persistence paths.
