---
status: accepted
---

# Use one explicitly refreshed folder per Library

Each Library is a recursive snapshot of one user-selected root folder. The user refreshes it to reconcile new, changed, moved, and missing files. The first release omits live filesystem watching and Libraries assembled across folders so scans, permissions, and recovery remain predictable.
